name: build

on:
  push:
    branches: [ "main" ]

jobs:
  job1:
    name: job
    runs-on: ubuntu-latest
    steps:
      - name: First step
        shell: bash
        run: |
          echo ${{ github.run_id }}

  job2:
    name: job2
    runs-on: ubuntu-latest
    steps:
      - name: Second first step
        shell: bash
        run: |
          echo ${{ github.run_id }}
